# Quartz.NET Example Project
This project demonstrates the basic usage of Quartz.NET in a .NET console application. It includes examples of manual triggering and scheduling a job using a cron expression.
